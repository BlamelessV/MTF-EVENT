-- ============================================================
-- shared.lua — Пространство имён MTFHeli
-- Загружается первым. Все модули добавляют сюда свой код.
-- ============================================================

MTFHeli = MTFHeli or {}

-- Колбэки, которые устанавливает адаптер конкретного геймода (heli_breach.lua)
-- SpawnPlayer(ply, roleData) → bool    Вызывается при высадке игрока из вертолёта
-- GetEnemyTeams() → {team1, team2}     Команды, которые боты считают врагами
-- GetRoles() → {role1, role2}          Список ролей для ботов
