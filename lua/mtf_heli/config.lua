-- ============================================================
-- config.lua — Все настройки вертолёта МОГ
-- Меняйте значения здесь, НЕ в других файлах.
-- ============================================================

MTFHeli.Config = {
    -- Модель вертолёта
    Model = "models/hh/veh/heli.mdl",

    -- Позиция парирования (куда вертолёт прилетает)
    HoverPos = Vector(-1143.817017, 8347.950195, 1939.040527),

    -- Смещение при вылете (относительно HoverPos, вперёд по Y и вверх по Z)
    ExitOffset = Vector(0, -8000, 1500),

    -- Угол поворота модели (градусы)
    ModelYaw = 180,

    -- Тайминги (секунды)
    HoldTime = 15,              -- Сколько вертолёт зависает над точкой
    DepartDuration = 12,        -- Сколько летит до выхода за карту
    PlayerSpawnDelay = 5,       -- Задержка спавна игроков (канат опускается)
    NotifyDelay = 2,            -- Задержка звукового оповещения mtf_enter

    -- Z-порог: игроки ниже (HOVER_POS.z - OutsideZ) считаются внутри комплекса
    -- Внутри комплекса роторные звуки НЕ слышны
    OutsideZ = 500,

    -- Лимиты
    MaxPlayers = 12,            -- Максимум игроков в высадке
    MaxBots = 24,               -- Максимум ботов через mtf_spawn_support

    -- Боты
    BotEnemyRange = 3000,       -- Радиус обнаружения врага
    BotMoveSpeed = 200,         -- Скорость движения к врагу
    BotFireInterval = 0.1,      -- Интервал стрельбы (секунды)
    BotSpawnResetDelay = 0,     -- Таймер повторной установки позиции бота (тики)

    -- Звуки: дистанция отключения при вылете
    DepartSoundCutoff = 3000,

    -- Точки высадки (смещения от HOVER_POS)
    DisembarkOffsets = {
        Vector(0, 0, 50),
        Vector(80, 0, 50),
        Vector(-80, 0, 50),
        Vector(0, 80, 50),
        Vector(0, -80, 50),
        Vector(60, 60, 50),
        Vector(-60, 60, 50),
        Vector(60, -60, 50),
        Vector(-60, -60, 50),
        Vector(140, 0, 50),
        Vector(-140, 0, 50),
        Vector(0, 140, 50),
        Vector(0, -140, 50),
        Vector(100, 100, 50),
        Vector(-100, 100, 50),
        Vector(100, -100, 50),
        Vector(-100, -100, 50),
        Vector(180, 0, 50),
        Vector(-180, 0, 50),
        Vector(0, 180, 50),
        Vector(0, -180, 50),
        Vector(130, 130, 50),
        Vector(-130, 130, 50),
        Vector(130, -130, 50),
    },
}
